import styled from 'styled-components';

const Wrapper = styled.div`
	width: 100%;
	height: 100%;
	background: hsla(200, 50%, 50%, 0.2);
`;

const Projects = () => {
	return <Wrapper>This is projects view</Wrapper>;
};

export default Projects;
